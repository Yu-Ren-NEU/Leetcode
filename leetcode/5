5. Longest Palindromic Substring
    刚开始用了brute force因为题中并未对时间复杂度设置需求。
    写了一个函数判断是否回文，并把子字符串分别放进函数中进行判断。
    submit后超过时限。
    
    看解析，得以下：

		寻找一个字符串中的最长回文子字符串，运用中心扩散寻找法，大大缩小寻找时间
		最关键的一个问题是回文字符串中心点的寻找，分为两种，一种是单个字符为中心，一种是双字符为中心。
